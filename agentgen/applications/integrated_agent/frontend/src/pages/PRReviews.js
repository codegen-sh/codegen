import React from 'react';
import { Box, Typography } from '@mui/material';

function PRReviews() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        PR Reviews
      </Typography>
      <Typography variant="body1">
        This page will display PR reviews performed by the agent and allow you to view the results.
      </Typography>
    </Box>
  );
}

export default PRReviews;